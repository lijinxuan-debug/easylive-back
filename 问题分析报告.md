# EasyLive 问题分析报告

## 问题 1：视频详情页参数为空错误

### 错误信息
```
javax.validation.ConstraintViolationException: getVideoInfo.videoId: 不能为空
请求地址:/video/getVideoInfo
```

### 问题分析

**错误原因**: 前端调用 `/video/getVideoInfo` 接口时，`videoId` 参数为空

**路由配置**：
```javascript
// router/index.js
path: '/video/:videoId'
name: 'videoDetail'
component: () => import('@/views/videoDetail/VideoDetail.vue')
```

**前端调用代码**：
```vue
// views/videoDetail/VideoPList.vue
const loadVideoPList = async () => {
    let result = await proxy.Request({
        url: proxy.Api.loadVideoPList,
        params: {
            videoId: route.params.videoId,  // ← 从路由获取
        },
    });
};
```

**可能原因**：
1. 路由参数 `route.params.videoId` 在某些情况下为空
2. 页面加载时序问题，参数未准备好就调用了接口
3. 某些边界情况参数传递异常

### 快速修复建议

**1. 添加参数校验**
```vue
const getVideoInfo = async () => {
    const videoId = route.params.videoId || route.query.videoId;
    if (!videoId || videoId === 'undefined') {
        proxy.Message.error('视频ID不能为空');
        return;
    }
    // 继续调用接口...
};
```

**2. 添加重试机制**
```vue
const getVideoInfo = async (retryCount = 3) => {
    for (let i = 0; i < retryCount; i++) {
        try {
            const result = await proxy.Request({...});
            if (result) return;
        } catch (e) {
            if (i === retryCount - 1) {
                throw e;
            }
        }
    }
};
```

---

## 问题 2：播放量未更新

### 症状
用户反馈：播放量"没用还是会一直动"

### 可能原因
1. **定时任务未运行** - 导致播放计数未更新到数据库
2. **Redis 队列消费失败** - 导致记录的播放信息未被处理
3. **方法调用错误** - 使用 `addReadCount`（阅读数）而非播放计数

### 代码分析
```java
// ExecuteQueueTask.java:83 行
videoInfoService.addReadCount(videoPlayInfoDto.getVideoId());

// 应该调用
redisComponent.recordPlayCount(videoPlayInfoDto.getVideoId());
```

### 快速验证

```bash
# 1. 查看播放计数
redis-cli KEYS "*VIDEO_PLAY_COUNT*"
redis-cli GET "VIDEO_PLAY_COUNT:2026-04-10:你的视频ID"

# 2. 查看数据库
SELECT video_id, play_count
FROM video_info
WHERE video_id = '你的视频ID'

# 3. 查看日志
tail -f logs/easylive-web.log | grep -i "video.*play"
```

### 修复方案

**在 FileController 的 videoResource 方法中添加播放计数**：
```java
@RequestMapping("/videoResource/{fileId}")
public void videoResource(HttpServletResponse response, @PathVariable("fileId") @NotEmpty String fileId) {
    // ... 厰有代码 ...

    // 添加这一行：
    TokenUserInfoDto userInfoDto = getTokenUserInfoFromCookie();
    if (userInfoDto != null) {
        // 每次播放时增加计数
        redisComponent.recordPlayCount(videoInfoFile.getVideoId());
    }
}
```

---

## 快速排查步骤

1. **检查定时任务** - 是否正常运行
2. **检查 Redis** - 队列是否有消费记录
3. **检查方法名** - `addReadCount` vs `recordPlayCount`
4. **手动测试** - 直接调用播放接口查看计数是否递增

---

**文档版本**: v1.0
**创建日期**: 2026-04-10
